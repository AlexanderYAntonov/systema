import { HasReliablePredictionPipe } from './has-reliable-prediction.pipe';

describe('HasReliablePredictionPipe', () => {
  it('create an instance', () => {
    const pipe = new HasReliablePredictionPipe();
    expect(pipe).toBeTruthy();
  });
});
